import { EsqueletoPanel } from "@/components/EsqueletoPanel";

/** Carga instantánea al navegar entre vistas del panel del vendedor. */
export default function CargandoVendedor() {
  return <EsqueletoPanel />;
}
