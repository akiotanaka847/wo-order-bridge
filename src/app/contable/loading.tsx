import { EsqueletoPanel } from "@/components/EsqueletoPanel";

/** Carga instantánea al navegar entre vistas del panel contable. */
export default function CargandoContable() {
  return <EsqueletoPanel />;
}
