import { EsqueletoPanel } from "@/components/EsqueletoPanel";

/** Carga instantánea al navegar entre vistas del panel admin. */
export default function CargandoAdmin() {
  return <EsqueletoPanel />;
}
